// PasakaApp.swift
import SwiftUI

@main
struct PasakaApp: App {
    // Выставляем горизонтальную ориентацию приложения для всех устройств
    init() {
        // Устанавливаем приложение только в горизонтальной ориентации
    }
    
    var body: some Scene {
        WindowGroup {
            MainView()
                .onAppear {
                                    // Убираем проблемную строку с UIDevice.orientation
                                    // Вместо этого используем Info.plist настройки
                                    print("🎯 Приложение запущено")
                                }
        }
    }
}
